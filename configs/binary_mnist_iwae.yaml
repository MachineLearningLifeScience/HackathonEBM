# Trains an IWAE model (fully observed binarized mnist)

defaults:
  - train: default
  - model: vae/mnist
  - data: prop_mnist
  - callbacks: image_ebm_debiasing_proportion

model:
  _target_: src.models.VAE
  latent_dim: 10