# Trains an IWAE model (fully observed binarized mnist)

defaults:
  - train: default
  - model: vae/binary_mnist
  - data: binary_mnist
  - callbacks: image_vae

model:
  _target_: src.models.MIWAE

train:
  epochs: 400